package Clase;

public class Main {
    public static void main(String[] args) {
        /*
            * Arimeticos:
              + -> suma
              - -> resta
              * -> multiplicación
              / -> división

             * Logicos
               > mayor que
               < menor que
               <> menor o mayor que
               != distinto que
               == igual que
               && y logico
               !! o logico

             * Incremento y Decremento
                ++ incremento
                -- decremento
                += n : incremento
                -= n : decremento

              * Concatenación:
                 + -> une o concatena

        */
    }
}